.text{ "data-message-id": "#{message.id}"}
  %h1
    =message.user.name
  %h2
    =message.created_at.strftime("%Y/%m/%d %H:%M") 
  .message
    %h3
      = message.body
    = image_tag message.image
